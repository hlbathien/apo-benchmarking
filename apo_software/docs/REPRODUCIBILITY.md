# Reproducibility

Level 1 validates the supplied 144-bundle corpus and verifies that every model
has one derived record per prompt. Level 2 recomputes model reliability and
failure-taxonomy tables without provider access. The generator is supplied for
inspection and fresh deterministic corpora; the private effective seed used for
the reported corpus is intentionally not included in the anonymous artifact.
